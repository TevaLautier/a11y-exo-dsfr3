import { AutoErrorMessageDirective } from './auto-error-message-directive';

describe('AutoErrorMessageDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoErrorMessageDirective();
    expect(directive).toBeTruthy();
  });
});
